language: python
notifications:
  email:
    on_success: never
    on_failure: always

sudo: required

services:
  - docker

python:
    - "3.6"

script:
  - python ./dockerlief/main.py list
  - python ./.github/main.py


cache:
  directories:
    - $HOME/docker
